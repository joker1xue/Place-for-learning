23.08.29

stack容器

/***基本概念*********************************************/

stack:栈

     _____    栈底     栈容器，符合先进后出，不允许遍历（遍历是非质变算法，栈只能对栈顶的元素进行访问）
    |_____|            可以判断是否为空（empty()），可以对容器内元素进行计数（size()）
    |_____|
    |_____|
    |     |   栈顶
push ↑   ↓ pop
入栈       出栈



/***常用接口*******************************************/

构造函数：
stack<T> stk;
stack(const stack& stk);

赋值操作：
stack& operator=(const stack& stk);

数据存取：
push(elem);
pop();
top();//返回栈顶元素

大小操作：
empty();
size();


